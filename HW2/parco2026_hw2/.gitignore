# --- build artifacts ---
serial
parallel
serial_timed
*.o

# --- run output (regenerated every job) ---
y_N*.txt
bench.*.out

# --- benchmark by-products (regenerated by parse_results.py) ---
bench_results.csv
figs/
logs/

# --- editor / OS ---
.vscode/
.idea/
.DS_Store
*.swp

# Per-version local bench logs (regenerated by ./bench_local.sh)
bench_v*.out
bench_otf*.out
bench_final.out
bench_local.out

# Local-only binary alongside parallel and serial
parallel_otf
